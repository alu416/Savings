<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.com.savings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Entergoal"
            android:label="@string/title_activity_entergoal"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="android.example.com.savings.MainActivity" />
        </activity>
        <activity android:name=".ShowProgress" />
        <activity android:name=".Settings" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".setSnacks" />
        <activity android:name=".setDrinks" />
        <activity android:name=".setEating" />
        <activity android:name=".customize1" />
        <activity android:name=".customize2" />
        <activity android:name=".customize3"></activity>
    </application>

</manifest>
